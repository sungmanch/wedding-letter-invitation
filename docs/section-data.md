# Section Data Definition

**Version:** 2.1
**Last Updated:** 2025-12-21
**Author:** BMad + Claude

---

## 데이터 구조 트리

```
📦 Wedding Invitation UserData
│
│ ════════════════════════════════════════════════════
│ ✅ 필수 섹션 (비활성화 불가)
│ ════════════════════════════════════════════════════
│
├── 📝 intro (인트로) ─────────────────── ✅ 필수 섹션
│   ├── photos.main ───────────── ✅ 필수 (3:4 비율)
│   ├── couple.groom.name ─────── ✅ 필수 ◆ 원본
│   ├── couple.groom.nameEn ───── ○ 선택 (영문 이름)
│   ├── couple.bride.name ─────── ✅ 필수 ◆ 원본
│   ├── couple.bride.nameEn ───── ○ 선택 (영문 이름)
│   ├── wedding.date ──────────── ✅ 필수 ◆ 원본
│   └── intro.message ─────────── ○ 선택
│
├── 💌 greeting (인사말/혼주정보) ────────── ✅ 필수 섹션 🔄
│   │
│   │  ── 인사말 ──
│   ├── greeting.title ────────── ○ 선택
│   ├── greeting.content ──────── ○ 선택
│   │
│   │  ── 혼주정보 ──
│   ├── parents.deceasedIcon ─── ○ 선택 (故/고/✿)
│   ├── parents.groom
│   │   ├── father
│   │   │   ├── name ──────────── ○ 선택
│   │   │   ├── nameEn ────────── ○ 선택 (영문)
│   │   │   ├── status ────────── ○ 선택 (표기 없음/故)
│   │   │   ├── baptismalName ─── ○ 선택
│   │   │   └── phone ─────────── ○ 선택
│   │   └── mother (동일 구조)
│   ├── parents.bride (동일 구조)
│   │
│   │  ── 연락처 ──
│   ├── → couple.groom.name ──── (intro 참조)
│   ├── couple.groom.phone ────── ○ 선택
│   ├── → couple.bride.name ──── (intro 참조)
│   ├── couple.bride.phone ────── ○ 선택
│   └── contact.showParents ───── ○ 선택
│
├── 💒 date (예식일시) ───────────────── ✅ 필수 섹션
│   ├── → wedding.date ────────── (intro 참조)
│   ├── wedding.time ──────────── ✅ 필수
│   ├── wedding.dateDisplay ───── ⚙️ 자동 (한글)
│   ├── wedding.dateEn ────────── ⚙️ 자동 (영문)
│   ├── wedding.timeDisplay ───── ⚙️ 자동 (한글)
│   ├── wedding.timeEn ────────── ⚙️ 자동 (영문)
│   ├── wedding.dday ──────────── ⚙️ 자동
│   ├── countdown.* ───────────── ⚙️ 자동
│   ├── → couple.groom.name ──── (intro 참조, calendar)
│   └── → couple.bride.name ──── (intro 참조, calendar)
│
├── 📍 venue (오시는길) ──────────────── ✅ 필수 섹션
│   ├── venue.name ────────────── ✅ 필수
│   ├── venue.nameEn ──────────── ○ 선택 (영문)
│   ├── venue.hall ────────────── ○ 선택
│   ├── venue.address ─────────── ⚙️ 자동
│   ├── venue.addressEn ───────── ○ 선택 (영문)
│   ├── venue.lat/lng ─────────── ⚙️ 자동
│   ├── venue.tel ─────────────── ○ 선택
│   ├── venue.naverUrl ────────── ○ 선택
│   ├── venue.kakaoUrl ────────── ○ 선택
│   ├── venue.tmapUrl ─────────── ○ 선택
│   └── venue.transportation
│       ├── bus ───────────────── ○ 선택
│       ├── subway ────────────── ○ 선택
│       ├── shuttle ───────────── ○ 선택
│       ├── parking ───────────── ○ 선택
│       └── etc ───────────────── ○ 선택 (기타 교통정보) 🆕
│
├── 🖼️ gallery (갤러리) ───────────────── ✅ 필수 섹션
│   ├── photos.gallery ────────── ○ 선택 (max: 40장)
│   └── gallery.effect ────────── ○ 선택
│
│ ════════════════════════════════════════════════════
│ ⭐ 권장 섹션 (기본 활성화, 비활성화 가능)
│ ════════════════════════════════════════════════════
│
├── 💰 accounts (축의금계좌) ─────────── ⭐ 권장 섹션
│   ├── accounts.groom[] ──────── ○ 선택 (max: 3)
│   │   └── [item]
│   │       ├── relation ──────── ✅ 필수 (항목 내)
│   │       ├── bank ──────────── ✅ 필수 (항목 내)
│   │       ├── number ────────── ✅ 필수 (항목 내)
│   │       └── holder ────────── ✅ 필수 (항목 내)
│   ├── accounts.bride[] (동일 구조)
│   └── accounts.kakaopay
│       ├── groom ─────────────── ○ 선택
│       └── bride ─────────────── ○ 선택
│
│ ════════════════════════════════════════════════════
│ ○ 선택 섹션 (기본 비활성화, 활성화 가능)
│ ════════════════════════════════════════════════════
│
├── 👫 couple (신랑신부 소개/인터뷰) ───── ○ 선택 섹션 🆕
│   ├── couple.photo ──────────── ○ 선택 (커플 사진)
│   ├── couple.groom.intro ────── ○ 선택 (신랑 소개)
│   ├── couple.groom.introEn ──── ○ 선택 (신랑 소개 영문)
│   ├── couple.bride.intro ────── ○ 선택 (신부 소개)
│   ├── couple.bride.introEn ──── ○ 선택 (신부 소개 영문)
│   └── couple.interview[] ────── ○ 선택 (max: 5)
│       └── [item]
│           ├── question ──────── ✅ 필수 (항목 내)
│           ├── questionEn ────── ○ 선택 (영문)
│           ├── answer ────────── ✅ 필수 (항목 내)
│           └── answerEn ──────── ○ 선택 (영문)
│
├── 📨 rsvp (RSVP) ───────────────────── ○ 선택 섹션
│   ├── rsvp.title ────────────── ○ 선택
│   ├── rsvp.titleEn ──────────── ○ 선택 (영문)
│   ├── rsvp.description ──────── ○ 선택
│   ├── rsvp.descriptionEn ────── ○ 선택 (영문)
│   ├── rsvp.showGuestCount ───── ○ 선택
│   ├── rsvp.showMeal ─────────── ○ 선택
│   ├── rsvp.showMessage ──────── ○ 선택
│   ├── rsvp.showSide ─────────── ○ 선택 (신랑측/신부측) 🆕
│   ├── rsvp.showBusOption ────── ○ 선택 (버스 탑승 여부) 🆕
│   ├── rsvp.deadline ─────────── ○ 선택
│   │
│   │  ── 개인정보처리방침 (필수) 🆕 ──
│   ├── rsvp.privacyPolicyUrl ─── ○ 선택 (방침 링크)
│   └── rsvp.privacyPolicyText ── ○ 선택 (동의 문구)
│
├── 📋 notice (공지사항) ─────────────── ○ 선택 섹션
│   └── notice.items[] ────────── ○ 선택 (max: 10)
│       └── [item]
│           ├── title ─────────── ✅ 필수 (항목 내)
│           ├── titleEn ───────── ○ 선택 (영문)
│           ├── content ───────── ✅ 필수 (항목 내)
│           ├── contentEn ─────── ○ 선택 (영문)
│           ├── icon ──────────── ○ 선택
│           ├── image ─────────── ○ 선택 (공지 이미지) 🆕
│           └── imagePosition ─── ○ 선택 (top/bottom) 🆕
│
├── 📖 guestbook (방명록) ─────────────── ○ 선택 섹션
│   ├── guestbook.title ───────── ○ 선택 (섹션 제목) 🆕
│   ├── guestbook.placeholder ─── ○ 선택 (입력창 플레이스홀더) 🆕
│   ├── guestbook.requireName ─── ○ 선택 (이름 필수 여부) 🆕
│   ├── guestbook.maxLength ───── ○ 선택 (메시지 최대 길이) 🆕
│   └── guestbook.messages ────── ⚙️ DB 조회
│
├── 🏁 ending (축하화환/공유/엔딩) ─────── ○ 선택 섹션 🔄
│   ├── ending.message ────────── ○ 선택
│   ├── ending.messageEn ──────── ○ 선택 (영문)
│   ├── ending.photo ──────────── ○ 선택
│   ├── ending.showCredit ─────── ○ 선택
│   │
│   │  ── 축하화환 ──
│   ├── ending.wreath
│   │   ├── enabled ───────────── ○ 선택 (활성화 여부)
│   │   ├── vendorUrl ─────────── ○ 선택 (외부 화환 업체 링크)
│   │   └── vendorName ────────── ○ 선택 (업체명)
│   │
│   │  ── 공유 ──
│   └── ending.share
│       ├── kakao ─────────────── ○ 선택 (카카오톡 공유)
│       ├── link ──────────────── ○ 선택 (링크 복사)
│       └── sms ───────────────── ○ 선택 (문자 공유)
│
├── 🎵 music (배경음악) ───────────────── ○ 선택 섹션
│   ├── bgm.trackId ───────────── ○ 선택
│   ├── bgm.title ─────────────── ○ 선택
│   └── bgm.artist ────────────── ○ 선택
│
└── 🎬 video (동영상) ─────────────────── ○ 선택 섹션 🔄
    ├── video.type ────────────── ○ 선택 (youtube/vimeo)
    ├── video.url ─────────────── ○ 선택
    ├── video.title ───────────── ○ 선택
    ├── video.autoplay ────────── ○ 선택
    └── video.muted ───────────── ○ 선택
```

**범례:**

| 구분 | 기호 | 의미 |
|------|------|------|
| **섹션** | ✅ 필수 섹션 | 비활성화 불가, 청첩장에 반드시 포함 |
| | ⭐ 권장 섹션 | 기본 활성화, 사용자가 비활성화 가능 |
| | ○ 선택 섹션 | 기본 비활성화, 사용자가 활성화 가능 |
| **필드** | ✅ 필수 | 반드시 입력해야 하는 필드 |
| | ○ 선택 | 선택적으로 입력 가능한 필드 |
| | ⚙️ 자동 | 시스템이 자동으로 계산/채우는 필드 |
| | ◆ 원본 | 공유 필드의 원본 정의 위치 |
| | → 참조 | 다른 섹션에서 정의된 필드 참조 |
| **태그** | 🆕 | 신규 추가된 섹션 |
| | 🔄 | 변경/확장된 섹션 |

### 공유 필드 요약

| 원본 필드 | 정의 섹션 | 참조 섹션 |
|----------|----------|----------|
| `couple.groom.name` | intro ✅ | greeting, date, couple |
| `couple.groom.nameEn` | intro ✅ | greeting, date, couple |
| `couple.bride.name` | intro ✅ | greeting, date, couple |
| `couple.bride.nameEn` | intro ✅ | greeting, date, couple |
| `wedding.date` | intro ✅ | date |

### 섹션 요약

| 구분 | 섹션 | 개수 |
|------|------|------|
| ✅ 필수 | intro, greeting, date, venue, gallery | 5개 |
| ⭐ 권장 | accounts | 1개 |
| ○ 선택 | couple, rsvp, notice, guestbook, ending, music, video | 7개 |
| **총계** | | **13개** |

---

### DB 테이블 구조 (RSVP 응답)

```
📊 rsvp_responses
│
├── id ──────────────────────── PK (uuid)
├── invitation_id ───────────── FK → superEditorInvitations
├── cookie_id ───────────────── ○ 익명 사용자 구분
├── guest_name ──────────────── ✅ 필수
├── guest_phone ─────────────── ○ 선택
├── attending ───────────────── ✅ 필수 (yes/no/maybe)
├── guest_count ─────────────── ○ 선택 (default: 1)
├── meal_option ─────────────── ○ 선택
├── side ────────────────────── ○ 선택 (groom/bride)
├── bus_required ────────────── ○ 선택 (버스 탑승 여부) 🆕
├── message ─────────────────── ○ 선택
├── privacy_agreed ──────────── ✅ 필수 (개인정보 동의) 🆕
└── submitted_at ────────────── ⚙️ 자동 (timestamp)
```

---

## 1. 섹션 개요

### 총 섹션 구성: 13개

| 구분 | 개수 | 섹션 목록 |
|------|------|----------|
| ✅ **필수** (비활성화 불가) | 5개 | intro, greeting, date, venue, gallery |
| ⭐ **권장** (기본 활성화) | 1개 | accounts |
| ○ **선택** (기본 비활성화) | 7개 | couple, rsvp, notice, guestbook, ending, music, video |

### 섹션 순서 기본값 (sectionOrder)

```typescript
const DEFAULT_SECTION_ORDER = [
  'intro',      // 1. 인트로
  'greeting',   // 2. 인사말/혼주정보
  'couple',     // 3. 신랑신부 소개/인터뷰
  'date',       // 4. 예식일시
  'gallery',    // 5. 갤러리
  'rsvp',       // 6. RSVP
  'venue',      // 7. 오시는길
  'notice',     // 8. 공지사항
  'accounts',   // 9. 축의금계좌
  'guestbook',  // 10. 방명록
  'ending',     // 11. 축하화환/공유/엔딩
  'music',      // 12. 배경음악 (FAB)
  'video',      // 13. 동영상
]
```

---

## 2. 섹션별 데이터 정의

### 2.1 Intro (인트로) ✅ 필수 섹션

**목적:** 청첩장 첫인상, 커플 대표 이미지와 기본 정보 표시

| 필드 | 타입 | 필수 | 기본값 | 설명 |
|------|------|------|--------|------|
| `photos.main` | image | ✅ | - | 메인 사진 (3:4 비율) |
| `couple.groom.name` | text | ✅ | - | 신랑 이름 (한글) |
| `couple.groom.nameEn` | text | ❌ | - | 신랑 이름 (영문) |
| `couple.bride.name` | text | ✅ | - | 신부 이름 (한글) |
| `couple.bride.nameEn` | text | ❌ | - | 신부 이름 (영문) |
| `wedding.date` | date | ✅ | - | 결혼 날짜 (ISO 형식) |
| `intro.message` | text | ❌ | "저희 두 사람이 사랑으로 하나가 됩니다" | 인트로 문구 |

**Variants:**
- `minimal` - 최소한의 정보만 표시
- `elegant` - 우아한 레이아웃
- `romantic` - 로맨틱한 분위기

**스타일 프리셋:** Cinematic, Exhibition, Magazine, Gothic Romance, Old Money, Monogram, Jewel Velvet, Custom

---

### 2.2 Greeting (인사말/혼주정보) ✅ 필수 섹션 🔄

**목적:** 인사말 + 혼주 정보 + 연락처 통합 섹션

#### 인사말 필드

| 필드 | 타입 | 필수 | 기본값 | 설명 |
|------|------|------|--------|------|
| `greeting.title` | text | ❌ | "저희 결혼합니다" | 인사말 제목 |
| `greeting.content` | textarea | ❌ | (5줄 기본 템플릿) | 인사말 내용 |

#### 혼주정보 필드 (기존 parents 섹션 통합)

| 필드 | 타입 | 필수 | 기본값 | 설명 |
|------|------|------|--------|------|
| `parents.deceasedIcon` | select | ❌ | "故" | 고인 표시 (故/고/✿) |
| `parents.groom.father.name` | text | ❌ | - | 신랑 아버지 이름 (한글) |
| `parents.groom.father.nameEn` | text | ❌ | - | 신랑 아버지 이름 (영문) |
| `parents.groom.father.status` | select | ❌ | "" | 신랑 아버지 상태 (표기 없음/故) |
| `parents.groom.father.baptismalName` | text | ❌ | - | 신랑 아버지 세례명 |
| `parents.groom.father.phone` | phone | ❌ | - | 신랑 아버지 연락처 |
| `parents.groom.mother.*` | - | ❌ | - | (동일 구조) |
| `parents.bride.father.*` | - | ❌ | - | (동일 구조) |
| `parents.bride.mother.*` | - | ❌ | - | (동일 구조) |

#### 연락처 필드 (기존 contact 섹션 통합)

| 필드 | 타입 | 필수 | 기본값 | 설명 |
|------|------|------|--------|------|
| `couple.groom.phone` | phone | ❌ | - | 신랑 연락처 |
| `couple.bride.phone` | phone | ❌ | - | 신부 연락처 |
| `contact.showParents` | boolean | ❌ | false | 혼주 연락처 표시 여부 |

**Variants:**
- `simple` - 텍스트만 표시
- `elegant` - 카드 형태
- `traditional` - 전통적 혼주 정보 레이아웃

---

### 2.3 Date (예식일시) ✅ 필수 섹션

**목적:** 결혼식 날짜/시간 및 D-day 카운트다운

| 필드 | 타입 | 필수 | 기본값 | 설명 |
|------|------|------|--------|------|
| `wedding.date` | date | → | - | 예식 날짜 (intro에서 참조) |
| `wedding.time` | time | ✅ | "14:00" | 예식 시간 |
| `wedding.dateDisplay` | text | 자동 | - | 한글 표시 (2025년 3월 15일 토요일) |
| `wedding.dateEn` | text | 자동 | - | 영문 표시 (March 15, 2025) |
| `wedding.timeDisplay` | text | 자동 | - | 한글 시간 (오후 2시) |
| `wedding.timeEn` | text | 자동 | - | 영문 시간 (2:00 PM) |
| `wedding.dday` | number | 자동 | - | D-day 계산값 |
| `countdown.days` | number | 자동 | - | 남은 일수 |
| `countdown.hours` | number | 자동 | - | 남은 시간 |
| `countdown.minutes` | number | 자동 | - | 남은 분 |
| `countdown.seconds` | number | 자동 | - | 남은 초 |

**Variants:**
- `countdown` - 카운트다운 강조
- `calendar` - 달력 형태 표시

---

### 2.4 Venue (오시는길) ✅ 필수 섹션

**목적:** 예식장 위치, 지도, 교통 정보

| 필드 | 타입 | 필수 | 기본값 | 설명 |
|------|------|------|--------|------|
| `venue.name` | text | ✅ | "그랜드 웨딩홀" | 예식장 이름 (한글) |
| `venue.nameEn` | text | ❌ | - | 예식장 이름 (영문) |
| `venue.hall` | text | ❌ | "3층 그랜드볼룸" | 홀 이름 |
| `venue.address` | text | 자동 | - | 주소 (LocationField에서 자동 채움) |
| `venue.addressEn` | text | ❌ | - | 영문 주소 |
| `venue.lat` | number | 자동 | - | 위도 |
| `venue.lng` | number | 자동 | - | 경도 |
| `venue.tel` | phone | ❌ | - | 예식장 전화번호 |
| `venue.naverUrl` | url | ❌ | - | 네이버 지도 공유 링크 |
| `venue.kakaoUrl` | url | ❌ | - | 카카오맵 공유 링크 |
| `venue.tmapUrl` | url | ❌ | - | T맵 공유 링크 |
| `venue.transportation.bus` | textarea | ❌ | - | 버스 정보 |
| `venue.transportation.subway` | textarea | ❌ | - | 지하철 정보 |
| `venue.transportation.shuttle` | textarea | ❌ | - | 셔틀버스 정보 |
| `venue.transportation.parking` | textarea | ❌ | - | 주차 안내 |
| `venue.transportation.etc` | textarea | ❌ | - | 기타 교통정보 🆕 |

**Variants:**
- `map-focus` - 지도 중심 레이아웃

---

### 2.5 Gallery (갤러리) ✅ 필수 섹션

**목적:** 웨딩 사진 갤러리

| 필드 | 타입 | 필수 | 기본값 | 설명 |
|------|------|------|--------|------|
| `photos.gallery` | images | ❌ | - | 갤러리 이미지 (최대 40장) |
| `gallery.effect` | select | ❌ | "slide" | 캐러셀 전환 효과 |

**gallery.effect 옵션:**
- `slide` - 슬라이드
- `fade` - 페이드
- `coverflow` - 커버플로우
- `cards` - 카드 스택
- `cube` - 큐브

**Variants:**
- `grid` - 3열 그리드 (간격 있음)
- `grid-seamless` - 3열 그리드 (여백 없음, 인스타그램 스타일)
- `masonry` - 2열 메이슨리
- `carousel` - 4:3 캐러셀 (자동재생 4초)
- `vertical-swipe` - 3:4 세로 스와이프

---

### 2.6 Accounts (축의금계좌) ⭐ 권장 섹션

**목적:** 축의금 계좌 정보

| 필드 | 타입 | 필수 | 기본값 | 설명 |
|------|------|------|--------|------|
| `accounts.groom` | repeater | ❌ | - | 신랑측 계좌 (최대 3개) |
| `accounts.bride` | repeater | ❌ | - | 신부측 계좌 (최대 3개) |
| `accounts.kakaopay.groom` | text | ❌ | - | 신랑측 카카오페이 계좌번호 |
| `accounts.kakaopay.bride` | text | ❌ | - | 신부측 카카오페이 계좌번호 |

**accounts.groom / accounts.bride 하위 필드:**

| 필드 | 타입 | 필수 | 설명 |
|------|------|------|------|
| `relation` | select | ✅ | 관계 (본인/아버지/어머니) |
| `bank` | text | ✅ | 은행명 |
| `number` | text | ✅ | 계좌번호 |
| `holder` | text | ✅ | 예금주 |

**Variants:**
- `tabs` - 탭 형태 (신랑측/신부측)

---

### 2.7 Couple (신랑신부 소개/인터뷰) ○ 선택 섹션 🆕

**목적:** 신랑/신부 프로필 및 Q&A 인터뷰

| 필드 | 타입 | 필수 | 기본값 | 설명 |
|------|------|------|--------|------|
| `couple.photo` | image | ❌ | - | 커플 사진 |
| `couple.groom.intro` | textarea | ❌ | - | 신랑 소개 (한글) |
| `couple.groom.introEn` | textarea | ❌ | - | 신랑 소개 (영문) |
| `couple.bride.intro` | textarea | ❌ | - | 신부 소개 (한글) |
| `couple.bride.introEn` | textarea | ❌ | - | 신부 소개 (영문) |
| `couple.interview` | repeater | ❌ | - | Q&A 인터뷰 (최대 5개) |

**couple.interview 하위 필드:**

| 필드 | 타입 | 필수 | 설명 |
|------|------|------|------|
| `question` | text | ✅ | 질문 (한글) |
| `questionEn` | text | ❌ | 질문 (영문) |
| `answer` | textarea | ✅ | 답변 (한글) |
| `answerEn` | textarea | ❌ | 답변 (영문) |

**인터뷰 질문 예시:**
- 첫인상은 어땠나요?
- 프러포즈는 어떻게 했나요?
- 상대방의 좋은 점은?
- 결혼 후 하고 싶은 것은?

**Variants:**
- `profile` - 프로필 중심
- `interview` - Q&A 중심
- `combined` - 프로필 + Q&A

---

### 2.8 RSVP (참석여부) ○ 선택 섹션

**목적:** 하객 참석 여부 수집 및 관리

| 필드 | 타입 | 필수 | 기본값 | 설명 |
|------|------|------|--------|------|
| `rsvp.title` | text | ❌ | "참석 여부를 알려주세요" | RSVP 제목 (한글) |
| `rsvp.titleEn` | text | ❌ | "Please RSVP" | RSVP 제목 (영문) |
| `rsvp.description` | textarea | ❌ | - | 안내 문구 (한글) |
| `rsvp.descriptionEn` | textarea | ❌ | - | 안내 문구 (영문) |
| `rsvp.showGuestCount` | boolean | ❌ | true | 동행인 수 입력 받기 |
| `rsvp.showMeal` | boolean | ❌ | false | 식사 여부 입력 받기 |
| `rsvp.showMessage` | boolean | ❌ | true | 메시지 입력란 표시 |
| `rsvp.showSide` | boolean | ❌ | false | 신랑측/신부측 선택 표시 🆕 |
| `rsvp.showBusOption` | boolean | ❌ | false | 버스 탑승 여부 표시 🆕 |
| `rsvp.deadline` | date | ❌ | - | 마감일 |
| `rsvp.privacyPolicyUrl` | url | ❌ | - | 개인정보처리방침 링크 🆕 |
| `rsvp.privacyPolicyText` | text | ❌ | "개인정보 수집 및 이용에 동의합니다" | 동의 문구 🆕 |

**참고:** 개인정보를 수집하는 경우 `privacyPolicyUrl` 설정 필수

**Variants:**
- `popup` - 팝업 형태
- `inline` - 인라인 폼

---

### 2.9 Notice (공지사항) ○ 선택 섹션

**목적:** 셔틀버스, 주차, 식사 등 안내사항

| 필드 | 타입 | 필수 | 기본값 | 설명 |
|------|------|------|--------|------|
| `notice.items` | repeater | ❌ | - | 공지사항 목록 (최대 10개) |

**notice.items 하위 필드:**

| 필드 | 타입 | 필수 | 설명 |
|------|------|------|------|
| `title` | text | ✅ | 공지 제목 (한글) |
| `titleEn` | text | ❌ | 공지 제목 (영문) |
| `content` | textarea | ✅ | 공지 내용 (한글) |
| `contentEn` | textarea | ❌ | 공지 내용 (영문) |
| `icon` | select | ❌ | 아이콘 (bus/car/utensils/info/gift/clock) |
| `image` | image | ❌ | 공지 이미지 (메뉴판, 노선도 등) 🆕 |
| `imagePosition` | select | ❌ | 이미지 위치 (top/bottom, 기본: bottom) 🆕 |

**Variants:**
- `list` - 목록형 (아이콘 + 제목 + 내용)
- `accordion` - 접기형 (클릭 시 펼침)
- `card` - 카드형 (그리드 배치)

---

### 2.10 Guestbook (방명록) ○ 선택 섹션

**목적:** 게스트 축하 메시지

| 필드 | 타입 | 필수 | 기본값 | 설명 |
|------|------|------|--------|------|
| `guestbook.title` | text | ❌ | "축하 메시지를 남겨주세요" | 섹션 제목 🆕 |
| `guestbook.placeholder` | text | ❌ | "축하 메시지를 입력해주세요" | 입력창 플레이스홀더 🆕 |
| `guestbook.requireName` | boolean | ❌ | true | 이름 필수 여부 🆕 |
| `guestbook.maxLength` | number | ❌ | 500 | 메시지 최대 길이 🆕 |
| `guestbook.messages` | - | 자동 | - | DB에서 조회 (에디터 입력 불필요) |

**Variants:**
- `fab` - 플로팅 버튼 (스크롤 후 표시)
- `block` - 인라인 블록 형태

---

### 2.11 Ending (축하화환/공유/엔딩) ○ 선택 섹션 🔄

**목적:** 청첩장 마무리 + 축하화환 주문 + 공유 기능

#### 기본 필드

| 필드 | 타입 | 필수 | 기본값 | 설명 |
|------|------|------|--------|------|
| `ending.message` | textarea | ❌ | "와주셔서 감사합니다" | 엔딩 메시지 (한글) |
| `ending.messageEn` | textarea | ❌ | "Thank you for coming" | 엔딩 메시지 (영문) |
| `ending.photo` | image | ❌ | - | 엔딩 사진 |
| `ending.showCredit` | boolean | ❌ | true | "Made with Maison de Letter" 표시 |

#### 축하화환 필드 🆕

| 필드 | 타입 | 필수 | 기본값 | 설명 |
|------|------|------|--------|------|
| `ending.wreath.enabled` | boolean | ❌ | false | 축하화환 기능 활성화 |
| `ending.wreath.vendorUrl` | url | ❌ | - | 외부 화환 업체 링크 |
| `ending.wreath.vendorName` | text | ❌ | - | 화환 업체명 |

#### 공유 기능 필드 🆕

| 필드 | 타입 | 필수 | 기본값 | 설명 |
|------|------|------|--------|------|
| `ending.share.kakao` | boolean | ❌ | true | 카카오톡 공유 버튼 |
| `ending.share.link` | boolean | ❌ | true | 링크 복사 버튼 |
| `ending.share.sms` | boolean | ❌ | true | 문자 공유 버튼 |

**Variants:**
- `simple` - 엔딩 메시지만
- `full` - 메시지 + 화환 + 공유

---

### 2.12 Music (배경음악) ○ 선택 섹션

**목적:** BGM 플레이어

| 필드 | 타입 | 필수 | 기본값 | 설명 |
|------|------|------|--------|------|
| `bgm.trackId` | select | ❌ | "romantic-piano-01" | BGM 프리셋 ID |
| `bgm.title` | text | ❌ | - | 곡 제목 |
| `bgm.artist` | text | ❌ | - | 아티스트명 |

**bgm.trackId 옵션:**
- `romantic-piano-01` - 로맨틱 피아노 1
- `romantic-piano-02` - 로맨틱 피아노 2
- `elegant-orchestra-01` - 우아한 오케스트라
- `playful-acoustic-01` - 경쾌한 어쿠스틱
- `emotional-ballad-01` - 감동적인 발라드
- `classical-canon` - 클래식 - 캐논

**Variants:**
- `fab` - 플로팅 플레이어 (우측 하단)
- `inline` - 인라인 플레이어

---

### 2.13 Video (동영상) ○ 선택 섹션 🔄

**목적:** 웨딩 영상 임베드

| 필드 | 타입 | 필수 | 기본값 | 설명 |
|------|------|------|--------|------|
| `video.type` | select | ❌ | "youtube" | 영상 타입 (youtube/vimeo) |
| `video.url` | url | ❌ | - | YouTube/Vimeo URL |
| `video.title` | text | ❌ | - | 영상 제목 |
| `video.autoplay` | boolean | ❌ | false | 자동 재생 |
| `video.muted` | boolean | ❌ | true | 음소거 시작 |

**Variants:**
- `fullwidth` - 전체 너비
- `card` - 카드 형태

---

## 3. 데이터 수집 시점

| 시점 | 수집 데이터 |
|------|------------|
| **Stage 1 (폼)** | `couple.groom.name`, `couple.bride.name`, `wedding.date`, `wedding.time`, `venue.name` |
| **Stage 2 (AI 채팅)** | 분위기 선호, 색상 선호 → AI가 스타일 생성 |
| **Edit (편집 페이지)** | 그 외 모든 상세 정보 |

---

## 4. 자동 계산 필드 (__HIDDEN__)

에디터에서 숨김 처리되며 시스템이 자동으로 계산하는 필드:

| 필드 | 계산 방식 |
|------|----------|
| `wedding.dateDisplay` | `wedding.date` → "2025년 3월 15일 토요일" |
| `wedding.timeDisplay` | `wedding.time` → "오후 2시" |
| `wedding.dateEn` | `wedding.date` → "March 15, 2025" |
| `wedding.timeEn` | `wedding.time` → "2:00 PM" |
| `wedding.dday` | 현재 날짜 기준 D-day 계산 |
| `wedding.month` | `wedding.date` → "3" |
| `wedding.day` | `wedding.date` → "15" |
| `wedding.weekday` | `wedding.date` → "토" |
| `countdown.days` | 실시간 카운트다운 |
| `countdown.hours` | 실시간 카운트다운 |
| `countdown.minutes` | 실시간 카운트다운 |
| `countdown.seconds` | 실시간 카운트다운 |
| `venue.address` | LocationField에서 주소 검색 시 자동 채움 |
| `venue.lat` | LocationField에서 주소 검색 시 자동 채움 |
| `venue.lng` | LocationField에서 주소 검색 시 자동 채움 |
| `guestbook.messages` | DB에서 조회 |

---

## 5. 한글/영문 병기 필드 정리

| 섹션 | 한글 필드 | 영문 필드 |
|------|----------|----------|
| **intro** | `couple.groom.name` | `couple.groom.nameEn` |
| | `couple.bride.name` | `couple.bride.nameEn` |
| **greeting** | `parents.*.father.name` | `parents.*.father.nameEn` |
| | `parents.*.mother.name` | `parents.*.mother.nameEn` |
| **date** | `wedding.dateDisplay` | `wedding.dateEn` |
| | `wedding.timeDisplay` | `wedding.timeEn` |
| **venue** | `venue.name` | `venue.nameEn` |
| | `venue.address` | `venue.addressEn` |
| **couple** | `couple.groom.intro` | `couple.groom.introEn` |
| | `couple.bride.intro` | `couple.bride.introEn` |
| | `couple.interview[].question` | `couple.interview[].questionEn` |
| | `couple.interview[].answer` | `couple.interview[].answerEn` |
| **rsvp** | `rsvp.title` | `rsvp.titleEn` |
| | `rsvp.description` | `rsvp.descriptionEn` |
| **notice** | `notice.items[].title` | `notice.items[].titleEn` |
| | `notice.items[].content` | `notice.items[].contentEn` |
| **ending** | `ending.message` | `ending.messageEn` |

---

## 6. 변경 이력

| 날짜 | 버전 | 변경 내용 | 작성자 |
|------|------|----------|--------|
| 2025-12-21 | 2.1 | 피드백 반영 | Claude |
| | | - venue.transportation.etc (기타 교통정보) 추가 | |
| | | - 공지사항 이미지 필드 추가 (image, imagePosition) | |
| | | - 방명록 설정 옵션 추가 (title, placeholder, requireName, maxLength) | |
| | | - RSVP 확장 (showSide, showBusOption, privacyPolicy) | |
| | | - DB: bus_required, privacy_agreed 컬럼 추가 | |
| 2025-12-21 | 2.0 | 섹션 구조 전면 재설계 | Claude |
| | | - parents, contact 섹션 → greeting에 통합 | |
| | | - couple (신랑신부 소개/인터뷰) 섹션 신규 추가 | |
| | | - ending 섹션 확장 (축하화환, 공유 기능) | |
| | | - video 섹션 Post-MVP → MVP 승격 | |
| | | - 한글/영문 병기 필드 전면 추가 | |
| | | - 총 14개 → 13개 섹션으로 정리 | |
| 2025-12-14 | 1.0 | 초기 작성 - 14개 섹션 정의, 경쟁사 분석 기반 | BMad + Claude |
